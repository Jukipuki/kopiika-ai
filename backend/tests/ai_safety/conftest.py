"""Placeholder for shared fixtures introduced by Story 10.8b's runner.

Story 10.8a ships only the corpus + a schema-validation unit test, so no
pytest markers are registered here (the global ``eval`` and ``integration``
markers from ``backend/pyproject.toml`` are inherited). When 10.8b lands the
runner it will add fixtures here for the live-agent invocation harness.
"""
